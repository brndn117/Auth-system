import React from 'react';

const Footer = () => {
  return (
    <footer style={styles.footer}>
      <p>© 2025 TrueSite. All rights reserved.</p>
    </footer>
  );
};

const styles = {
  footer: {
    textAlign: 'center',
    padding: '20px',
    backgroundColor: '#333',
    color: 'white',
    marginTop: '40px',
  }
};

export default Footer;
// This code defines a simple Footer component for a React application. It includes a copyright notice and is styled with a dark background and white text. The styles are defined inline for simplicity, ensuring the footer is visually appealing and consistent with the overall design of the application.